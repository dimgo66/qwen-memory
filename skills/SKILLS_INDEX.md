# 📚 Skills Index

Автоматически загружается в начале каждой сессии.

## Активные skills

| Skill | Описание |
|-------|----------|
| `git` | Git операции (commit, push, branch, merge) |
| `docker` | Docker контейнеры (build, run, compose) |
| `database` | SQL запросы (PostgreSQL, MySQL) |
| `api` | HTTP запросы (curl, REST, GraphQL) |
| `files` | Файловые операции (read, write, search) |
| `nodejs` | Node.js (npm, yarn, scripts) |
| `python` | Python (venv, pip, pytest) |
| `react` | React (components, hooks, state) |
| `typescript` | TypeScript (types, interfaces, generics) |
| `backend` | Backend разработка (API, auth, database) |
| `frontend` | Frontend (UI, CSS, responsive) |
| `fullstack` | Fullstack интеграция |
| `devops` | CI/CD, деплой, мониторинг |
| `cloud` | Облачные сервисы (AWS, GCP, Azure) |
| `testing` | Тесты (unit, integration, e2e) |
| `design` | UI/UX принципы |
| `accessibility` | Доступность (a11y, WCAG) |
| `responsive` | Адаптивный дизайн |
| `performance` | Оптимизация скорости |
| `security` | Безопасность (auth, encryption) |
| `ai-integration` | AI интеграции |
| `graphql` | GraphQL запросы |
| `microservices` | Микросервисная архитектура |
| `webscraping` | Парсинг сайтов |
| `memory` | Долгосрочная память |
| `postgres-memory` | PostgreSQL память |
| `thinking` | Структурированное мышление |
| `remember` | Сохранение в память (`/remember`) |

## Быстрые команды

```bash
/memory          # Показать память
/remember <факт> # Сохранить в память
```

## MCP серверы (автоподключены)

- ✅ `filesystem` — файловая система
- ✅ `memory` — долгосрочная память
- ✅ `postgres` — PostgreSQL доступ
- ✅ `puppeteer` — браузер
- ✅ `sequentialthinking` — мышление
- ✅ `git` — Git операции
